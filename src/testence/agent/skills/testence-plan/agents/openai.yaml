interface:
  display_name: "Testence Plan"
  short_description: "Design a reviewable Testence PlanSpec"
  default_prompt: "Use $testence-plan to turn this feature risk into a validated Testence PlanSpec."
policy:
  allow_implicit_invocation: true
