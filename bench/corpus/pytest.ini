; A rootdir of its own, on purpose.
;
; A consuming project's settings may describe external environments and pin a
; test-id attribute for its application. Discovered from here it would quietly
; reconfigure the corpus, so the corpus runs with its own rootdir and takes
; everything from the environment the runner sets.
[pytest]
testpaths = .
